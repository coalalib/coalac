
#define EXPIRATION_TIME 1000
#define DEFAULT_WINDOW_SIZE 300
#define MIN_WINDOW_SIZE 50
#define MAX_WINDOW_SIZE 15000
#define MAX_ATTEMPS 6

#define MIN(a,b) (((a)<(b))?(a):(b))
#define MAX(a,b) (((a)>(b))?(a):(b))
